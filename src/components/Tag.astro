---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<p>
  <a
    class="link"
    href={`/tags/${name}`}
    aria-label={`Blog posts tagged with ${name}`}
  >
    #{name}
  </a>
</p>